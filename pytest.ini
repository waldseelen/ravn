# pytest konfigürasyonu

[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -p no:django

markers =
    slow: Yavaş çalışan testler
    integration: Entegrasyon testleri
    skip: Atlanması gereken testler
