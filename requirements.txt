customtkinter>=5.0.0
Pillow>=9.0.0
yt-dlp
pytest>=7.0
requests>=2.31.0
click>=8.0.0
mutagen>=1.45.0

# Torrent / magnet support
# aria2c binary: install via `winget install aria2` (Windows), `brew install aria2` (macOS), `apt install aria2` (Linux)
aria2p>=0.12.1

# Desktop App & Auto-Update
pyinstaller>=5.0

# Phase 5: Advanced UI Features (Optional)
tkinterdnd2>=0.3.0  # Drag & Drop (Windows)
pystray>=0.19.0     # System tray
# win10toast>=0.9     # Windows notifications (currently unused)
